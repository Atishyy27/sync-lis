node_modules/

# local wrangler dev state (sqlite DO storage, bundled temp files)
.wrangler/

# the server's self-signed TLS keypair — never leaves this machine
.cert/

# audio fetched for the queue, and generated test media
media/
test/fixture/*.mp4
test/fixture/*.m4a

# local scratch
*.log
.DS_Store

# packaged extension zips, rebuilt by CI on every tag
dist/

# meant to stay local (session handoff notes) — was accidentally tracked
SESSION-CONTEXT.local.md
